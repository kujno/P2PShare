<Window x:Class="P2PShare.InviteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:P2PShare"
        mc:Ignorable="d"
        Title="Invite" Height="225" Width="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#333333"
        BorderThickness="1px"
        MouseDown="Window_MouseDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Grid.Row="0" Margin="0, 30px, 0, 0" MaxHeight="125px" VerticalAlignment="Center">
            <TextBlock x:Name="Text" FontSize="20px" FontFamily="Arial" Foreground="#c2c2c2" HorizontalAlignment="Center" Margin="0" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" TextAlignment="Center" VerticalAlignment="Center"/>
        </ScrollViewer>
        <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Margin="0, 0, 0, 45px">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#292929"/>
                    <Setter Property="Foreground" Value="#c2c2c2"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Height" Value="20px"/>
                    <Setter Property="Width" Value="70px"/>
                </Style>
            </StackPanel.Resources>
            
            <Button x:Name="No" Content="No" Margin="0" Click="No_Click"/>
            <Button x:Name="Yes" Content="Yes" Margin="70px, 0, 0, 0" Click="Yes_Click"/>
        </StackPanel>
    </Grid>
</Window>
